<!-- BEGIN: Login Form -->
<div class="h-screen xl:h-auto flex py-5 xl:py-0 my-10 xl:my-0">
    <div class="my-auto mx-auto xl:ml-20 bg-white dark:bg-darkmode-600 xl:bg-transparent px-5 sm:px-8 py-8 xl:p-0 rounded-md shadow-md xl:shadow-none w-full sm:w-3/4 lg:w-2/4 xl:w-auto">
        <h2 class="intro-x font-bold text-2xl xl:text-3xl text-center xl:text-left">
            Sign In
        </h2>
        <div class="intro-x mt-2 text-slate-400 xl:hidden text-center">A few more clicks to sign in to your account. Manage all your e-commerce accounts in one place</div>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class='intro-x mt-8'>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "intro-x login__input form-control py-3 px-4 block", placeholder:"Email" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "intro-x login__input form-control py-3 px-4 block mt-4", placeholder: "Password" %>
          </div>
          <div class="intro-x flex text-slate-600 dark:text-slate-500 text-xs sm:text-sm mt-4">
              <% if devise_mapping.rememberable? %>
                <div class="flex items-center mr-auto">
                  <%= f.check_box :remember_me, class: "form-check-input border mr-2" %>
                  <%= f.label :remember_me, class:"cursor-pointer select-none" %>
                </div>
              <% end %>
              <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                <%= link_to "Forgot your password?", new_password_path(resource_name) %><br />
              <% end %>
          </div>
          <div class="intro-x mt-5 xl:mt-8 text-center xl:text-left">
              <%= f.submit "Log in", class:"btn btn-primary py-3 px-4 w-full xl:w-32 xl:mr-3 align-top" %>
              <button class="btn btn-outline-secondary py-3 px-4 w-full xl:w-32 mt-3 xl:mt-0 align-top">Register</button>
          </div>
          <div class="intro-x mt-10 xl:mt-24 text-slate-600 dark:text-slate-500 text-center xl:text-left"> By signin up, you agree to our <a class="text-primary dark:text-slate-200" href="">Terms and Conditions</a> & <a class="text-primary dark:text-slate-200" href="">Privacy Policy</a> </div>
        <% end %>
        <%= render "devise/shared/links" %>
    </div>
</div>
<!-- END: Login Form -->
